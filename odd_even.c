#include<stdio.h>
int main()
{
int x;
printf("enter the number");
scanf("%d",&x);
if (x%2==0)
{
    printf( " number entered is an even number ");
    }
else {
printf( " number entered is not an even number");
}

return 0;
}